# 2026-08-26 gpupro6000c 设备日志

> **设备归属**：gpupro6000c（本机，lfwang@gpupro6000c，1×RTX PRO 6000 Blackwell 98GB）
> **分设备日志起始**：从 8-26 起按设备分写，本文件记录 gpupro6000c 上发生的事项。
> **设备拓扑**：gpupro6000 系列共 **a/b/c/d/e 五台**，共享数据和 conda 环境（NFS），处理器与显卡独立。
> 他机事项（gpupro6000a/b/d/e、h100 等）各自记入对应设备日志。

## 今日任务

- [x] C 类（`tiantianx/smolvla_libero`）mj332 四 suite 重跑启动（PID **488174**）
- [ ] C 类重跑结果回填（预计 ~21h，8-27 中午前后）

## 设备归属（今日涉及）

| 任务 | 设备 | 说明 |
|---|---|---|
| C 类 mj332 重跑（本次） | **gpupro6000c** | 本机，PID 488174，15:58 启动 |
| A 类 mj332 重跑 | gpupro6000d（同 NFS 上 a 机 15:40 亦启动，见 a 机日志） | 8-26 启动，PID 297118 |
| B 类 mj332 重跑 | gpupro6000a | 8-26 启动，PID 2892109，卡 HF 下载中 |
| fresh-expert 100k 训练 | gpupro6000d | 8-25 启动，PID 4159989，仍在运行 |

## 1. C 类四 suite 重跑（进行中）

- 脚本：`scripts/run_phase6_mj332_C_foursuite.sh`（POLICY=`tiantianx/smolvla_libero`，16 层 / 0.75 宽 / paper-like 复现候选）
- 启动：15:58:18（日志首条 WARNING 时间戳），nohup 后台，**PID 488174**
- 日志：`outputs/table2_repro_audit/06_simulator/mj332_C_run.log`
  - ⚠️ 注意：回显命令写的是 `mj332_2_C_run.log`，**实际落盘为此文件名 `mj332_C_run.log`**（与 A 类 `mj332_2_A_run.log` 的命名习惯不同，引用时以实际文件为准）
- 输出：`outputs/table2_repro_audit/06_simulator/mj332_C_<suite>/eval_info.json`（幂等，已有结果跳过）
- 已确认生效（日志 config 段）：`n_action_steps=10`、`n_episodes=50`、`--rename_map`（image→camera1, image2→camera2）
- 启动时快照：尚未出现 "Stepping through eval batches"（首个 task 未完成），spatial 评测准备中

### ⚠️ 本次重跑相对 8-25 首轮 C 类（mj332）的协议变更（重要）

除 MuJoCo 环境（3.8.1→3.3.2，8-25 已改）外，**C 脚本本次同样修改了两个参数**（与 A/B 类脚本已统一）：

| 参数 | 旧值（8-25 / 3.8.1 基线） | 新值（本次） | 影响 |
|---|---|---|---|
| `--policy.n_action_steps` | 1 | **10** | 动作执行节奏变化（对齐 paper recipe / A/B 类新协议） |
| `--eval.n_episodes` | 10 | **50** | 每 task 评测集数 10 → 50（方差更小，结果更稳） |

**影响与注意**：

1. **结果不可直接比较**：本次为 n_action_steps=10 / n_episodes=50 协议，与 3.8.1 下 C 类基线（S76/O59/G63/L40 = 59.5%，n_action_steps=1 / n_episodes=10）**协议不同**，不可并表，需按协议分组；
2. **样本量扩大 5 倍**：每 suite 500 集（10 task × 50 ep），四 suite 共 2000 集，为上次（400 集）的 5 倍；
3. **与 A/B 类可比**：四脚本（A/B/C）已统一 n_action_steps=10、n_episodes=50，本次 C 类结果可与 A/B 类 mj332 重跑直接对比；**D 类脚本仍未改**（`n_action_steps=1`、`n_episodes=10`），若要与 A/B/C 对比需先统一协议；
4. **耗时大幅增加**：上次 400 集约 4.2h，本次 2000 集预计 **~21h**，最早 8-27 中午前后完成。

```bash
# 查看进度（每个 task 完成更新一行）
grep -a "Stepping through eval batches" outputs/table2_repro_audit/06_simulator/mj332_C_run.log | tail -3
# 进程
ps -p 488174 -o pid,etime,cmd
```

## 结论与下一步

### 进行中
1. [ ] C 类四 suite 跑完后回填汇总（预计 8-27 中午前后；协议 n_action_steps=10 / n_episodes=50，与历史 59.5% 不可直接对比，但与 A/B 类 mj332 重跑可比）
2. [ ] D 类脚本协议统一决策（当前仍为旧协议 n_action_steps=1 / n_episodes=10，与 A/B/C 不可比）

### 备查
- C 类 3.8.1 基线：S76/O59/G63/L40 = 59.5%；需 rename_map（config camera3 stale，无需补）
- 各机 8-26 mj332 重跑均为 mujoco 3.3.2（gpupro6000 系列 `smolvla_eval` 本身即是 3.3.2）
- 沙盒环境本会话无法直接执行终端命令，命令均由用户在 gpupro6000c 本地终端执行
