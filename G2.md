我刚检查了仓库当前 `main`。现在状态很明确：**G0、G1 已完成，G2 是最高优先级，但 G2 文档与框架代码还没有完全同步到 G1 的结论，因此现在不应该直接跑 G2-B，而应该先完成一个“小而严格”的 tensor-scale framework Gate。**

当前父实验已经记录 G1 的 `88 / 21 / 21 / 84%`，并明确写出 F3 崩溃来自 VLM W4；G2 被标记为下一 Gate。 G1 的 task-wise 数据也已经很干净：VLM-only W4 完整复现全 W4 的失败，而 Expert-only W4 基本恢复。

## 一、仓库当前有两个需要先处理的问题

第一是**G2 setup 文档已经落后于 G1 结果**。当前 `weight-granularity/docs/experiment_setup.md` 还写着 G2-A/G2-B/...“其余同 G1-B”，也就是全 VLM+Expert W4，并且脚本计划一次顺序跑 5 个 config。它同时又写了“若 G1 定位单一组件，可以只扫该组件”，但还没有真正回填这个 Gate。([GitHub][1])

现在应正式改成：

| 组    | VLM Linear            | Expert Linear | 状态                  |
| ---- | --------------------- | ------------- | ------------------- |
| G2-A | per-tensor W4         | raw FP        | **直接复用 G1-C = 21%** |
| G2-B | per-output-channel W4 | raw FP        | 下一正式实验              |
| G2-C | group-128 W4          | raw FP        | conditional         |
| G2-D | group-64 W4           | raw FP        | conditional         |
| G2-E | group-32 W4           | raw FP        | conditional         |

因为 G1-C 的真实配置正是 `linear.include: vlm.*`、Expert raw FP、W4 + E4M3 A/O + `pot_ao_outlier`，Goal ×100，完全可以作为 G2-A anchor。

第二是**framework 现在确实没有做到 tensor-aware weight scale**。目前：

`QuantizedLinear.w_interval` 仍是 `Optional[float]`；`quant_bias()` 直接假定 `a_interval * w_interval` 可作为标量；scale 文件只是 pickle load/save，没有 shape/granularity 校验。

`QuantStatistics` 仍定义 `w_scales: List[float]`，多个 calibration sample 最终用 Python `max(self.w_scales)`；保存和打印也假定 scale 可以用 `:.6f` 格式化。

`scale_methods.py` 甚至直接把 `per_channel` 标成了 reserved placeholder。

而 forward 中的 `M_aw`、`M_fa_qb` 目前也是 scalar 语义，在完整 `F.linear()` 后直接乘。

所以你的判断是对的：**现在必须先还这笔代码债务。**

---

# 二、建议把 G2 拆成 5 个严格 Gate

整体执行顺序固定为：

```text
G2-P0a  旧代码 golden snapshot
          ↓
G2-P0b  tensor-scale framework refactor
          ↓
G2-P0c  scalar/raw regression
          ↓
G2-P1   per-output-channel W4 + numerical preflight
          ↓
G2-B    Goal × 100 closed-loop
```

只有 G2-B 有明显恢复后，再开发 groupwise。

---

# 三、G2-P0a：改代码前先保存旧实现 golden

这个步骤现在就应该先做，否则代码改完后没有可靠的“旧行为参照”。

在：

```text
experiments/2026-09-10_phaseG_w4-root-cause/
└── tasks/weight-granularity/
    └── scripts/
```

新增：

```text
capture_scalar_golden.py
```

它使用现有 **G1-C config + G1-C 已校准 scalar scales**，固定：

```python
torch.manual_seed(1234)
torch.cuda.manual_seed_all(1234)
```

取同一个 calibration batch，分别保存：

```text
raw_predict_action_chunk.pt
quant_predict_action_chunk.pt
```

以及至少 3 个代表 Linear 的：

```text
vlm.layers.0.self_attn.q_proj
vlm.layers.0.self_attn.v_proj
vlm.layers.0.mlp.down_proj
```

保存：

```text
input
weight
bias
a_interval
w_interval
o_interval
raw_output
quant_output
```

原始产物放：

```text
outputs/2026-09-10_phaseG_w4-root-cause/
└── tasks/weight-granularity/
    └── framework-preflight/
        └── scalar-golden/
```

**这一份一定要在代码重构前生成。**

---

# 四、G2-P0b：第一笔 commit 只做 tensor-scale infrastructure

这一 commit **不要实现 per-channel 算法**。

目标只有一个：

> 让 framework 能安全地保存、加载、聚合、校验和传递 scalar 或 tensor `w_interval`，同时旧 scalar path 数值完全不变。

建议 commit：

```text
quant: make linear weight scales tensor-aware
```

需要改 5 个核心文件。

### `quant_linear.py`

把：

```python
self.w_interval: Optional[float] = None
```

改成逻辑上的：

```python
Scale = float | torch.Tensor

self.w_interval: Scale | None = None
self.weight_quant_granularity = "per_tensor"
```

这里不要简单到处写：

```python
if torch.is_tensor(...)
```

建议建立统一 helper，例如：

```python
def _weight_scale_for_weight(self):
    """
    scalar              -> scalar
    [Nout] per-channel  -> [Nout, 1]
    """
```

以及：

```python
def _weight_scale_for_output(self, out):
    """
    scalar -> scalar

    [Nout] ->
        [1, Nout]        for [B, Nout]
        [1, 1, Nout]     for [B, T, Nout]
        ...
    """
```

这是这一轮最重要的设计点。

因为当前 `quant_awo()` 虽然已经支持 Tensor scale，但它只是直接执行：

```python
x / scale
```

并依赖 broadcasting。

如果 weight 是：

$$
W:[N_{out},K]
$$

你直接传：

```python
scale.shape == [Nout]
```

PyTorch 会把 `[Nout]` 对齐到 **最后一维 K**，这是错误的。

因此必须明确：

```text
logical per-channel scale = [Nout]

weight quantization view  = [Nout, 1]

output rescale view       = [1, ..., 1, Nout]
```

不要让 `quant_awo()` 自己猜维度。

---

# 五、`stat_manager.py` 必须真正 tensor-aware

当前：

```python
self.w_scales: List[float]
...
max(self.w_scales)
```

需要改成例如：

```python
Scale = float | torch.Tensor
self.w_scales: list[Scale]
```

采集时 tensor 一律：

```python
scale.detach().to(torch.float32).cpu().clone()
```

最终聚合不能使用 Python `max()`。

建议新增统一函数：

```python
def reduce_scale_max(scales):
    if all scalar:
        return max(scales)

    if tensor:
        assert all shapes identical
        result = scales[0].clone()
        for s in scales[1:]:
            result = torch.maximum(result, s)
        return result
```

这正好保持当前 calibration 的原始语义：

> across calibration samples 取 maximum

只不过从 scalar max 扩展成：

$$
s^{final}_{w,j}
=
\max_t s^{(t)}_{w,j}
$$

对于后续 groupwise 则自然扩展成：

$$
s^{final}_{w,j,g}
=
\max_t s^{(t)}_{w,j,g}.
$$

另外这两个地方也必须改：

```python
print(f"{scale_value:.6f}")
```

Tensor 不能这么打印。

建议：

```text
scalar: value=...
tensor: shape=(...), min=..., max=..., mean=...
```

当前保存逻辑和格式化逻辑确实都还假设 scalar。

---

# 六、scale persistence 不建议继续“只看文件在不在”

这是当前另一个真正的隐患。

现在：

```python
def _scale_files_exist(self):
    return all(os.path.exists(...))
```

然后：

```text
calibration_policy=auto
文件存在 → reuse
```

根本不看 scale 是 scalar 还是 `[Nout]`。

所以如果你以前的：

```text
q_proj_w_scale_0.p
```

是一个 scalar，而现在 G2-B 请求：

```text
per_output_channel
```

当前框架会有机会错误 reuse。

建议新增：

```python
def _validate_loaded_scales(self):
```

至少校验：

```text
per_tensor:
    w_interval = scalar / 0-d tensor

per_output_channel:
    w_interval.shape == [out_features]

groupwise:
    暂时直接 NotImplementedError
```

并检查：

```text
isfinite
> 0
dtype
shape
```

更推荐同时新增 metadata sidecar：

```text
<scale_group>_scale_meta_<idx>.p
```

内容：

```python
{
    "format_version": 2,
    "weight_quant_granularity": "per_output_channel",
    "weight_scale_shape": [960],
    "in_features": 960,
    "out_features": 960,
    "w_spec": "int4",
    "method": "pot_ao_outlier",
}
```

兼容策略建议：

```text
旧 per_tensor scale:
    无 meta → 允许 legacy reuse

per_channel config:
    无 meta 或 scalar scale → 不允许 reuse
```

`auto` 最合理的行为是：

```text
files missing
    → recalibrate

files exist + compatible
    → reuse

files exist + incompatible
    → recalibrate
```

而显式：

```yaml
calibration_policy: reuse
```

遇到 incompatibility 应直接报错。

当前 `calibration.py` 只验证 reuse 层是否“文件存在”，所以这里也要一起改。

---

# 七、`model_wrapper.py` 只增加一个正交字段

不要改变现有：

```yaml
linear_scale_granularity: per_site
```

它管理的是**物理 site 之间是否共享 scale**。当前实现定义得很清楚：global/per_component/per_layer/per_site。

新增：

```yaml
weight_quant_granularity: per_tensor
```

在 `create_quantized_linear()` 中：

```python
quant_layer.weight_quant_granularity = str(
    layer_config.get(
        "weight_quant_granularity",
        quant_config.get(
            "weight_quant_granularity",
            "per_tensor",
        ),
    )
).lower()
```

第一阶段只允许：

```text
per_tensor
per_output_channel
```

如果读取：

```text
groupwise
```

直接：

```python
raise NotImplementedError(...)
```

而不是先写一个假的 groupwise。

当前 `create_quantized_linear()` 已经负责将 config 中的 method/outlier 等属性写到 module，所以把 granularity 放这里最自然。

---

# 八、G2-P0c：framework refactor 后必须过 8 项 Gate

在还没有加入 per-channel 算法时，先验证：

| Test                             | PASS 条件          |
| -------------------------------- | ---------------- |
| raw 2D Linear                    | bit-exact        |
| raw 3D Linear                    | bit-exact        |
| raw bias=True                    | bit-exact        |
| raw bias=False                   | bit-exact        |
| scalar scale save→load           | 完全一致             |
| tensor scale save→load           | shape/value 完全一致 |
| stale scalar → per-channel reuse | **正确拒绝**         |
| G1-C scalar quant golden         | 与重构前一致           |

其中最关键的其实不是 raw。

因为 raw 当前就是：

```python
return F.linear(x, weight, bias)
```

本来就绕过 scale path。

真正关键的是：

$$
\boxed{
Y_{\text{old scalar quant}}
\equiv
Y_{\text{new scalar quant}}
}
$$

对之前保存的 golden，建议：

```text
FP32:
max_abs_diff == 0
```

如果某些设备 kernel 导致非严格 bit exact，则至少：

```text
max_abs_diff <= 1e-6
cosine >= 0.9999999
```

**这一 Gate 不过，禁止实现 G2-B。**

---

# 九、第二笔 commit 才实现真正的 per-output-channel W4

commit 建议：

```text
quant: add per-output-channel W4 calibration and forward
```

这里才修改 `scale_methods.py`。

G2-B 用的是：

```yaml
method: pot_ao_outlier
```

不是普通 `per_tensor`。G1-C 当前配置就是这个。

因此不能只实现一个孤立：

```python
scales_per_channel()
```

而要让：

```text
pot_ao_outlier
+
weight_quant_granularity=per_output_channel
```

正常工作。

对 calibration 中已经 mask 掉 outlier 的：

$$
W_{normal}\in R^{N_{out}\times K}
$$

计算：

$$
m_j=\max_k |W_{normal,j,k}|
$$

INT4：

$$
s_{w,j}=
\frac{m_j}{7.5}
$$

得到：

```text
logical w_interval.shape = [Nout]
```

如果某个 row 全为零：

```python
scale_j = 1.0
```

避免 0 / NaN。

A/O 仍然保持当前 scalar + PoT：

```text
a_interval: scalar PoT
o_interval: scalar PoT
w_interval: vector continuous
```

这完全符合 `pot_ao_outlier` 原来的定义：A/O PoT，weight continuous。

---

# 十、forward 必须修改四条 outlier 路径

当前 G1-C 走的是：

```python
quant_forward_pot_ao_outlier()
→ quant_forward_with_outlier()
```

后者现在计算：

```text
QA × QW
FA × QW
QA × FW
FA × FW
```

然后用 scalar `M_aw / M_fa_qb / M_qa_fb` 恢复 scale。

per-channel 后必须严格是：

$$
Y_{QA,QW,j}
=
s_a s_{w,j}
\sum_k q(a_k)q(w_{jk})
$$

$$
Y_{FA,QW,j}
=
s_{w,j}
\sum_k a^{FP}_kq(w_{jk})
$$

$$
Y_{QA,FW,j}
=
s_a
\sum_k q(a_k)w^{FP}_{jk}
$$

$$
Y_{FA,FW,j}
=
\sum_k a^{FP}_kw^{FP}_{jk}
$$

代码逻辑应是：

```python
w_scale_weight = [Nout, 1]
w_scale_output = [1, ..., Nout]
```

然后：

```python
w_sim = quant_awo(
    w_normal_fp,
    w_scale_weight,
    ...
)
```

以及：

```python
M_aw = a_scale * w_scale_output
M_fa_qb = w_scale_output
M_qa_fb = a_scale
```

不要直接把 `[Nout]` 丢给 `quant_awo()`。

---

# 十一、bias 路径也必须一起改

当前：

```python
bias / (a_interval * w_interval)
```

对于 per-channel 在数学上本身是正确的：

$$
q_{b,j}
=
round
\left(
\frac{b_j}{s_as_{w,j}}
\right)
$$

但必须显式确认：

```text
bias.shape      = [Nout]
w_scale logical = [Nout]
```

所以 `quant_bias()` 使用 logical `[Nout]` scale，而不是 `[Nout,1]` weight-view。

建议：

```python
def quant_bias(self, b):
    ws = self._weight_scale_vector()
    assert ws.ndim in (0, 1)

    if ws.ndim == 1:
        assert ws.numel() == self.out_features

    return round(b.float() / (self.a_interval * ws))
```

---

# 十二、G2-P1：闭环前先跑 numerical preflight

不要实现完立刻上 100 episodes。

先复用 G0 的思路，对 **112 个 VLM Linear** 同时计算：

```text
per_tensor W4
per_channel W4
```

至少生成：

```text
per_tensor_linear_stats.csv
per_channel_linear_stats.csv
granularity_comparison.csv
operator_summary.csv
layer_summary.csv
```

重点指标：

```text
weight NMSE
weight SQNR
weight cosine
output NMSE
output SQNR
quant zero ratio
saturation ratio
unique codes
scale min/max/mean
```

进入 G2-B 的硬 Gate 应为：

$$
mean(NMSE_{channel})
<
mean(NMSE_{tensor})
$$

且绝大部分 layer 满足：

$$
NMSE_{channel,i}
\le
NMSE_{tensor,i}.
$$

如果 per-channel 在很多层反而更差，首先判 implementation bug，不跑 LIBERO。

---

# 十三、通过后才创建真正的 G2-B config

直接复制 G1-C，只改这几个字段：

```yaml
# g2b_per_channel_vlm.yaml

output_dir: outputs/2026-09-10_phaseG_w4-root-cause/tasks/weight-granularity/g2b_per_channel_vlm

quantization:
  method: pot_fp8_outlier

  scale_dir: scales/2026-09-10_phaseG_w4-root-cause/weight-granularity/g2b_per_channel_vlm

  linear_scale_granularity: per_site

  weight_quant_granularity: per_output_channel

  linear:
    enabled: true
    include:
      - "vlm.*"
    exclude: []
```

七类 Linear 继续：

```yaml
w_bit: 4
a_bit: e4m3
o_bit: e4m3
method: pot_ao_outlier
outlier_ratio: 0.01
```

MatMul **一字不改**。

Evaluation：

```text
libero_goal
10 tasks × 10 episodes
seed=1000
n_action_steps=10
num_steps=10
```

因此唯一实验变量就是：

$$
\boxed{
VLM\ weight\ scale:
per\ tensor
\rightarrow
per\ output\ channel
}
$$

---

# 十四、G2-B 的决策标准

基准直接使用：

$$
G2A=G1C=21\%.
$$

我建议继续保留原来的 `+15pp` Gate，但实际按这个表决策：

| G2-B Goal SR | 下一步                                              |
| -----------: | ------------------------------------------------ |
|         ≥80% | granularity 基本确认主因 → 开发 groupwise 做 HW trade-off |
|       70–79% | granularity 为主要因素 → group128/64/32               |
|       50–69% | 部分解释 → groupwise + VLM selective precision       |
|       31–49% | 贡献有限 → 先做 groupwise offline，不急 rollout           |
|         ≤30% | per-channel 假设基本失败 → 停止 granularity 大扫描          |

所以**现在不应该实现 G2-C/D/E**。

---

## 当前最具体的执行顺序

```text
1. 更新 G2 experiment_setup.md
   - 改成 VLM-only
   - G2-A = reused G1-C 21%
   - G2-B first
   - groupwise conditional

2. 在旧代码上 capture_scalar_golden.py
   ↓

3. Commit A
   tensor-aware framework only
   - quant_linear.py
   - stat_manager.py
   - calibration.py
   - model_wrapper.py
   - scale persistence/reuse validation
   ↓

4. raw + scalar regression Gate
   必须 PASS
   ↓

5. Commit B
   per-output-channel W4
   - scale_methods.py
   - quant_methods.py
   - bias/broadcast helpers
   ↓

6. G2 numerical preflight
   112 VLM Linear
   ↓ PASS

7. g2b_per_channel_vlm
   Goal × 100
   ↓

8. 根据 SR 决定是否进入 groupwise
```

仓库当前其实已经非常适合这么做：`quant_awo()` 本身已经具备 Tensor-scale 数值运算基础，真正缺的是**scale shape 语义、广播、聚合、持久化与验证**。 所以这不是一次大规模重构，控制得好可以分成两个非常干净的 commit，而且不会破坏 G0/G1 已经固化下来的 scalar baseline。

我认为现在最关键的一条纪律是：**先保存旧 scalar golden，再开始改代码。** 否则等 tensor-aware 重构完成后，即使 G2-B 出现异常，也很难区分是 per-channel 算法的问题，还是你无意中改变了原来的 scalar quant path。

[1]: https://github.com/wolf111108/SmolVLA_qtrsc/blob/main/experiments/2026-09-10_phaseG_w4-root-cause/tasks/weight-granularity/docs/experiment_setup.md "SmolVLA_qtrsc/experiments/2026-09-10_phaseG_w4-root-cause/tasks/weight-granularity/docs/experiment_setup.md at main · wolf111108/SmolVLA_qtrsc · GitHub"
